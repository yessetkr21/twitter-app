import { Layout } from '../components/Layout';
import { Timeline } from '../components/Timeline';

const Index = () => {
  return (
    <Layout>
      <Timeline />
    </Layout>
  );
};

export default Index;
